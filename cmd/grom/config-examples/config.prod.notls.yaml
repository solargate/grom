server:
  name: "Grom"
  port: 8080
  tls:
    mode: off

federation:
  enabled: false

auth:
  jwt_secret: "REPLACE_WITH_STRONG_SECRET"
  jwt_ttl_hours: 24

storage:
  location: /var/lib/grom/data
  temp_dir: tmp

/// Opens Strava OAuth and returns the redirect URL (including query).
Future<String> authenticateStravaOAuth(String authorizationUrl) async {
  throw UnsupportedError('Strava OAuth is not supported on this platform');
}
